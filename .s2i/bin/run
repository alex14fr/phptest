#!/bin/sh

echo hello from run
pwd
ls
cp -a /persist/code/* .
cp -a /persist/code/.* .
ln -s /persist/data .
ln -s /persist/pecnum .
ls -la
> /opt/app-root/etc/conf.d/50-mpm-tuning.conf
exec httpd -DFOREGROUND
